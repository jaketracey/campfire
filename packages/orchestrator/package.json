{
  "name": "@campfire/orchestrator",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": ".venv/bin/python -m uvicorn orchestrator.main:app --host 0.0.0.0 --port 8000 --reload --app-dir src",
    "start": ".venv/bin/python -m uvicorn orchestrator.main:app --host 0.0.0.0 --port 8000 --app-dir src",
    "test": ".venv/bin/pytest",
    "lint": ".venv/bin/ruff check src",
    "typecheck": ".venv/bin/mypy src"
  }
}
